# Adapter Standard

Protocol adapters should expose read-only normalized views of markets, positions, fills or orders where available, funding state, oracle snapshots, liquidation eligibility inputs, protocol metadata, and known caveats.

Adapters must include protocol name, program IDs, account schema/version notes, data freshness, confidence level, and limitations.
