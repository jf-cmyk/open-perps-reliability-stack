# Continuous Research Roadmap

The loop advances one acceptance criterion at a time. A stage remains active until its evidence is source-backed and the next action is explicit.

## R0: Durable Memory And Cadence

Status: complete

Acceptance criteria:

- Compact hot state, evidence ledger, opportunity pipeline, and checkpoint archive exist.
- Five-minute runs are delta-gated and source-bounded.
- No-delta runs remain quiet.
- Ecosystem research and the separate validator-growth operating reports have distinct ownership.

## R1: Open Perps Call Evidence Matrix

Status: active

Checkpoints: `checkpoints/2026-07-22-open-perps-call-evidence-matrix.md`, `checkpoints/2026-07-22-jupiter-authority-recheck.md`, `checkpoints/2026-07-22-oprs-funding-fit.md`, `checkpoints/2026-07-22-drift-velocity-data-boundary.md`, `checkpoints/2026-07-22-phoenix-external-asset-perps.md`, and `checkpoints/2026-07-22-frontier-traders-oprs-channel.md`. The requirement matrix now includes the call's preference that application revenue be structurally routed back to Solana. OPRS should present its open, freely usable reliability layer as the standard-grant public good and keep commercial services separate or use the convertible-grant path; any future paid API can document Solana-native settlement through the verified subscriptions surface without implying current revenue. Phoenix's live external-asset market surface now provides a direct second venue target around market-hours state, composite mark inputs, funding, and liquidity. Phoenix production program and Hawkeye view constants are now pinned from the Ellipsis Labs Rise public source, while exact oracle identities, account-level decode, and replay remain gates. Frontier Traders adds a Foundation-backed, network-level path to opt-in professional-trader feedback and cross-venue reliability benchmarking, but no Blocksize access or demand is established. Jupiter remains blocked for canonical decoding and pairing. Drift's former Data API documentation and live legacy host now carry Velocity identity, so R1 advances by paging backward for a legacy onchain liquidation and treating hosted records as discovery rather than sole authority.

Source-governance priority: preserve Jupiter's semantic-only boundary. For legacy Drift reconstruction, the validated read-only paginator has now scanned 148,000 transactions from July 22 back through slot 418069879 at 2026-05-07T00:06:06Z without a `Liquidate*` log and stored the next cursor; this is bounded queue progress, not evidence that liquidations were absent. Resume from the stored cursor, preserve the pinned historical commit and program ID even where routes resolve to Velocity, require public Solana transaction evidence, and use migrated hosted data only as corroboration.

Goal: show exactly how OPRS answers the Solana Foundation call for fully onchain perps and complementary infrastructure.

Acceptance criteria:

- Each Foundation call requirement maps to a built artifact, public proof, owner, and gap.
- The free public-good core is separated from commercial extensions; any future application revenue documents Solana-native settlement without claiming current revenue or turning OPRS into a venue.
- Foundation-reported grant-program scale is 500+ projects, $100M+ in funding, and six continents, while the Foundation home frames grants around furthering censorship resistance. OPRS may connect transparent inclusion, landing, availability, and finality evidence to that focus, but must not claim it prevents censorship or proves censorship resistance.
- Drift, Jupiter, and Phoenix coverage is separated into verified, fixture-backed, and blocked states; Phoenix's public GOLD telemetry, mark-price semantics, production program ID, and Hawkeye view constants are source-pinned, while exact input identities, account-level decode, trader monitoring, and replay remain blocked.
- Grant-safe claims are distinguished from future commercial extensions.
- Jupiter lifecycle semantics are sourced from first-party docs; binary decode and verified pairing remain explicitly blocked pending canonical current IDL/source and exact derivation evidence.

## R2: Blocksize Opportunity Qualification

Status: active

Checkpoints: `checkpoints/2026-07-22-pye-blocksize-status.md`, `checkpoints/2026-07-22-blocksize-sdp-rpc-fit-gap.md`, `checkpoints/2026-07-22-sdp-compliance-error-boundary.md`, `checkpoints/2026-07-22-blocksize-pyth-publisher.md`, `checkpoints/2026-07-22-epicentral-opx-verification.md`, `checkpoints/2026-07-22-sanctum-blocksize-fit.md`, `checkpoints/2026-07-22-solblaze-blocksize-eligibility.md`, `checkpoints/2026-07-22-blocksize-doublezero-history.md`, `checkpoints/2026-07-22-frankendancer-doublezero-route-fix.md`, `checkpoints/2026-07-22-firedancer-v1-1-testnet.md`, `checkpoints/2026-07-22-pay-sh-gateway-boundary.md`, and `checkpoints/2026-07-22-termina-replay-api.md`. Pye's partner/support status is verified, while its Blocksize market remains unconfirmed. SDP RPC qualification is mapped behind an evidence packet, while SDP's provider-level compliance response and fail-closed rule establish a second bounded institutional reliability surface. Blocksize's 2023 Pyth data-provider relationship and legacy publisher identity are verified, but current activity must be re-established because the former Insights route redirects to generic Pyth Terminal Explore. Sanctum is a candidate branded-LST partner. BlazeStake now confirms both CLS eligibility and a public BLOCKSIZE selector/action path bound to the known vote account; aggregate realized usage remains unexposed. DoubleZero historical usage and payment are verified, advancing that opportunity to a controlled transport/performance benchmark. Frankendancer v0.1006.40100 adds a mainnet-ready client-version and route-state control; full Firedancer v1.1.x adds a separate testnet-only client stratum with new accounts, XDP, and DoubleZero/GRE changes. Blocksize's exact direct service route now returns a live x402 v2 challenge with Solana and Base USDC options, so payment rail becomes an explicit reliability and Open Perps revenue-routing dimension. Termina's replay API surface is verified, but its documented signature-disabled custom transactions and account overrides require canonical historical evidence to remain separate from counterfactual simulation output; access and fidelity remain unverified. Epicentral remains deferred.

Goal: turn ecosystem findings into partner/account motions with evidence, fit, and next steps.

Acceptance criteria:

- SDP RPC-provider criteria are mapped against public Blocksize evidence; missing endpoint, benchmark, operations, and customer-tenure proofs are explicit.
- Blocksize's first-party validator page claims more than 230,000 SOL under management, 99.99% uptime, zero slashing events, and SOC 1, SOC 2, and ISO/IEC 27001 standards compliance, while finalized RPC showed about 193,493 SOL activated on the named vote account. Treat these as different measures until stake composition, measurement windows, slashing scope, and attestation artifacts reconcile them; none currently clears an SDP hard gate. Keep the prior rank-290 full-set snapshot pinned to slot 434414574 and the newer filtered account read pinned to root slot 434555651 until rank is recomputed from a new full set.
- Pye relationship status is verified; market state is explicitly bounded as unconfirmed until a public lockup URL or owner dashboard proof is available. Blocksize's Solana validator page returned HTTP 404 across direct route variants on July 22 after an earlier indexed snapshot exposed its Speedstake callout; treat this only as source availability, not delisting, validator failure, market closure, or partnership termination.
- Termina's public replay API is mapped to a concrete Drift liquidation validation design with historical transactions, canonical signer/state evidence, signature-disabled custom transactions, and account overrides kept in separate evidence classes; API access, pricing, archive completeness, fidelity, and protocol-specific coverage remain gates. Phoenix still requires a concrete technical validation request.
- Epicentral SOS/OPX is excluded from near-term positioning unless canonical program, source, dependency, audit, and mainnet evidence appears.
- Sanctum's branded-LST opportunity remains a candidate until public Blocksize external-stake composition, threshold fit, economics, liquidity and concentration risks, and a read-only integration boundary are verified.
- BlazeStake CLS eligibility and public interface visibility are verified: the BLOCKSIZE row links to Blocksize and its Stake action binds the known vote account. Its documented read surface is user-address keyed, can lag one epoch, and returns targets that may differ from realized stake based on bSOL backing; no aggregate validator-balance endpoint is documented. Aggregate Blocksize target or applied balance, users, economics, endpoint behavior, and partnership status remain unverified.
- Blocksize's 2023 Pyth publisher relationship and legacy identity are verified; current activity must first be re-established before feed coverage, quality metrics, ranking, economics, or expansion positioning. The official Pythnet RPC page lists Triton, P2P, Blockdaemon, and Figment as access providers rather than publishing a public RPC URL, and publisher explorer guidance requires a custom RPC. Triton now has a verified provider-specific path: Pythnet RPC and Hermes endpoints come with subscriptions; account setup supplies endpoints and tokens, frontend endpoints may use origin/IP controls, and backend access can append a secret token. This does not establish Blocksize access or the terms of the other providers. The smallest next proof remains a current identity-bound metrics/conformance export or an already-authorized read-only provider observation.
- Blocksize's historical DoubleZero usage and fee payment are verified through epoch 938; current connection state and any latency, landing, shred-delivery, or market-quality benefit require a controlled read-only benchmark stratified by validator client/version and route state, including the Frankendancer v0.1006.40100 large-route-table packet-loss fix.
- SDP institutional proof packs distinguish screening-provider errors, stale or missing results, and disagreement from compliance decisions; the May 22 Chainalysis sandbox note remains dated evidence until current behavior is independently checked.
- SDP's ramp assessment adds settlement latency and SLA, API callbacks and sandbox readiness, support escalation, 24-hour material-incident notice, and devnet compatibility to the institutional proof-pack boundary. Current ramp integrations are described as bespoke and devnet-only, mainnet-promotion criteria are not yet defined, and listing is availability rather than endorsement.
- SDP's custodial-wallet assessment adds read-only API idempotency and error handling, webhook freshness, uptime and incident-history transparency, support escalation, material-incident notice, and periodic devnet compatibility to that boundary. Signing tests, private-key or custody access, key-management evaluation, custodian selection, and custody decisions remain excluded; all current integrations are devnet-only and listing is not endorsement.
- Every opportunity has a commercial score, confidence, owner type, and smallest next action.

## R3: Validator And Network Regime Readiness

Status: active

Checkpoints: `checkpoints/2026-07-22-blocksize-vat-bls-readiness.md`, `checkpoints/2026-07-22-sfdp-criteria-snapshot.md`, `checkpoints/2026-07-22-alpenglow-schedule-revision.md`, `checkpoints/2026-07-22-agave-4-3-alpha2.md`, `checkpoints/2026-07-22-frankendancer-doublezero-route-fix.md`, and `checkpoints/2026-07-22-firedancer-v1-1-testnet.md`. Blocksize BLS readiness and current stake position are verified. Agave 4.3 has advanced to alpha.2 with Votor/BLS work but remains prerelease software. Frankendancer v0.1006.40100 is labeled mainnet-ready and fixes outgoing packet loss under large route tables. Full Firedancer v1.1.x is explicitly testnet-only and introduces separate accounts, memory, XDP, DoubleZero/GRE, and shorter-slot variables. SFDP's forward schedules now make cluster and epoch part of the client-version gate, and Blocksize's known vote account did not match the public participant API snapshot; identity mapping or non-participation must be resolved before any SFDP fit claim. R3 also tracks the official August-to-October Alpenglow target discrepancy. Releases, targets, criteria, and API absence do not establish activation, rejection, or Blocksize readiness by themselves.

Client-version priority: public `getClusterNodes` returned one record for Blocksize identity `7VZM7YHcX73TpGoXDeBu61g4QKC86GwAEnew8dA7Y2xn` at finalized slot `434585668`, advertising `clientId` `AgaveBam`, version `4.1.1`, feature set `3345198602`, and shred version `50093`. This is a gossip-layer snapshot only; it does not prove installed binary provenance, configuration, DoubleZero routing, public RPC service, performance, SFDP qualification, VAT readiness, or future persistence.

Goal: build a defensible validator-pressure model around observable facts.

Acceptance criteria:

- VAT activation, BLS registration, admitted-set status, stake rank, delinquency, skipped slots, commission, stake trend, client version, route-table and GRE state, packet drops, Votor readiness, and Alpenswitch activation have public source paths.
- Blocksize/Pye validator status is checked without signing or custody access.
- SFDP criteria and required software schedules are tracked as cluster- and epoch-versioned data, including transaction-processing requirements; Blocksize's known vote account returned no public participant API match, so identity mapping or non-participation must be resolved before a fit-gap can imply program status.
- No named validator is described as failing without longitudinal evidence.

## R4: Ecosystem Coverage

Status: active

Capital-flow snapshots now cover July 21-22, 2026. Checkpoint: `checkpoints/2026-07-22-stablecoin-issuer-decomposition.md`. DefiLlama's daily chart attributes approximately $142.4M of its $143.9M daily increase to USDT and USDC and classifies the change as native-minted rather than bridged. Same-day refreshes invalidate direct surface comparison: the live Solana dollar-peg total and naive component sum first revised down together by approximately $195.9M, then rose together by approximately $794.35M to $16.006B and $16.620B, while the July 22 daily-chart point moved by less than $1M to $15.859B. The component-minus-live gap remained near $613.68M through the reversal, while chart-minus-live changed sign to about negative $147.49M. Pinned DefiLlama source partially reconciles the methodology: stablecoinchains excludes `doublecounted` assets and deduplicates normalized chain aliases, while the public stablecoincharts aggregate does not request that exclusion and the per-asset response omits the internal marker. Recompute a timestamp-pinned adjusted component total and align hourly/daily timestamps and prices before issuer-event, recipient, commercial, demand, or whale conclusions; these revisions are analytics-pipeline evidence, not capital movements.

Priority update: Checkpoints `checkpoints/2026-07-22-blocksize-pay-sh-provider.md` and `checkpoints/2026-07-22-pay-sh-gateway-boundary.md` verify that Blocksize Market Data is live in the official Pay.sh catalog with four metered x402 routes. The committed OpenAPI server is the direct Blocksize service, and an unpaid request returned x402 v2 exact-payment options on Solana mainnet and Base. A future OPRS commercial endpoint must report or constrain the settlement rail before claiming revenue is routed back to Solana. Solana's official Subscriptions documentation supplies canonical program `De1egAFMkMWZSN5rYXRj9CAdheBamobVNubTsi9avR44`, source and audit-provenance paths, and lifecycle events for recurring API tiers; current bytecode, audit delta, paid usage, and demand remain unverified. Solana Foundation's MIT-licensed Commerce Kit adds a merchant checkout and Solana Pay integration surface, while the official payment guide makes landed-payment verification and production indexing separate from a UI success callback. Its source is pinned at commit `6164d5104f3d1bd4cfbb637075f000d6ac23d6c3`; all six child package names at that commit resolve in the public npm registry at matching version labels despite the private root workspace and zero GitHub releases. Registry packages were last published on October 22, 2025, so tarball equivalence to the later pinned source, audits, deployment, maintenance, and adoption remain gates. SDP's official payment docs add a concrete institutional reconciliation surface: no hosted checkout or payment-intent object, no settlement webhook, and no inbound memo or Solana Pay reference in the Transfer record; applications correlate by per-order destination or direct RPC, poll status, and retain their own transfer mapping to prevent fresh-signature retries from paying twice. The action gate is now explicit: `confirmed` is for reversible low-stakes effects, while `finalized` is the safe signal for irreversible real-money effects; definitive timestamps and status consistency can be cross-checked by signature through Solana RPC, and documented polling ranges remain provisional defaults. Checkpoint `checkpoints/2026-07-22-moneygram-sdp-signal.md` establishes a large institutional validator and payments operator as an SDP participant, validating the buyer class for operational evidence without establishing Blocksize purchase intent. Checkpoints `checkpoints/2026-07-22-tokenized-equity-market-state.md` and `checkpoints/2026-07-22-phoenix-external-asset-perps.md` qualify representation-aware market state for 24/7 tokenized equities and gold/crude-oil perpetuals, respectively. Ondo's official API schema now supplies a concrete status and primary-versus-underlying adapter surface for that comparison, but its market-data prices are display-only, an official oracle is still in development, access requires an API key, and SKHYon coverage remains unverified. Checkpoint `checkpoints/2026-07-22-kamino-scope-v038.md` qualifies Scope oracle-transition and fallback observability around the new PythLazerEMA release, but both pinned release-commit mainnet JSON configs have zero exact PythLazerEMA entries; deployed configuration and public logs remain the evidence gate. Next productization priorities are one rail-aware Pay.sh reliability endpoint, a pinned Commerce Kit receipt-proof contract with tarball-provenance verification, a bounded SDP correlation/finality/retry/compliance and validator proof-pack specification, a schema-first SKHY-family comparison, Phoenix exact input/oracle verification plus Hawkeye local validation, and a Scope v0.38 onchain configuration/log verification before promoting those offers.

Foundation-initiative priority: qualify Frontier Traders as a review-only OPRS design-partner and benchmark channel before lower-confidence campaign work. The program spans spot and perpetual venues and offers high-tier members priority RPC and structured roadmap input; Blocksize membership, access, perps-specific participation, data sharing, and demand remain unverified. Legacy multi-validator and automated-rebalancing grant PDFs remain public but retain a March 1, 2022 deadline and are historical signals only; keep stake-authority custody and automated execution outside this research loop.

Issuer-source priority: Circle's public supply API reported `6,959,369,019.556649` USDC circulating on Solana at `2026-07-22T21:18:53.787912Z`. Use this as the primary USDC supply anchor and reconcile it against a same-time DefiLlama component; Circle states that Solana pre-mint balances are excluded until authorized into circulation. The snapshot is not a mint event, recipient, holder, whale, net inflow, venue-liquidity, or demand signal, and issuer-native Solana USDT remains open.

New-business-case priority: official SDP self-hosting now qualifies an adjacent operator-evidence offer. The operator supplies Solana RPC and runs a six-service stack, so rank read-only RPC/dependency health, version drift, upgrades, backup/restore, latency, availability, and failure-domain attribution after current OPRS venue work. Deployment, credentials, secrets, signing-provider tests, custody material, infrastructure changes, and production operation remain excluded.

Goal: maintain a ranked, current view of Solana protocols, capital flows, Foundation initiatives, partnerships, and business cases.

Acceptance criteria:

- Top protocols are ranked using clearly dated TVL, volume, revenue, user, and developer-activity definitions; Kamino now has a dated lending TVL/borrowed snapshot and tagged Scope release baseline, while a complete cross-metric ranking remains open.
- Whale and capital-flow signals are labeled as observed data or inference.
- DoubleZero, Alpenglow, Agave, Firedancer, BLT, Pye, Pyth publisher quality, and delegation programs have primary-source watch paths.
- New business cases are translated into Blocksize offers or rejected with reasons; Pay.sh distribution, canonical Solana subscription program/events, Commerce Kit checkout primitives and its separate landed-payment verification boundary, the SDP institutional buyer class, self-hosted SDP's operator-supplied RPC and multi-service operating boundary, and the tokenized-equity market-state reliability surface are verified, while Commerce Kit package/release provenance, deployed subscription bytecode equivalence, self-hosted SDP demand and operating outcomes, actual demand, and Blocksize customer relationships remain unproven.
- The official DvP educational design qualifies a no-signing, finalized two-leg receipt proof for Token-2022 asset and USDC payment transfers. Treat its `confirmed` result as provisional for irreversible evidence, verify both legs and authority/state context independently, and do not claim production readiness, legal sufficiency, audit completion, or counterparty-risk elimination beyond single-transaction atomicity.

## R5: Productization And Outreach Readiness

Status: active

Goal: convert validated opportunities into review-ready artifacts, without external outreach or writeback.

Acceptance criteria:

- One-page briefs exist for OPRS, SDP RPC qualification, VAT readiness, Pye, Pay.sh expansion, and replay partnerships.
- Each brief identifies buyer, pain, proof, offer, pilot, objection, and success metric.
- Outreach drafts remain review-only until separately approved.
- The public-good and commercial boundaries remain explicit.

## Loop Resolution Rule

Each material run must do exactly one of the following:

- verify a fact;
- close an evidence gap;
- change an opportunity score;
- produce a bounded technical implication;
- reject a weak opportunity;
- promote a stable conclusion into a checkpoint.

Afterward, update the queue and select the highest-value unresolved item. The loop should not create new questions faster than it closes them.
